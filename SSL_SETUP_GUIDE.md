# Пошаговая инструкция: Установка SSL-сертификата на Hoster.by

## Проблема
Касперский блокирует сайт из-за отсутствия или недействительного SSL-сертификата.

## Решение: Установка бесплатного SSL-сертификата Let's Encrypt

### Способ 1: Через панель управления Hoster.by

1. **Войдите в панель управления Hoster.by**
   - Откройте https://hoster.by
   - Войдите в личный кабинет

2. **Перейдите в раздел "Домены"**
   - Найдите в меню "Домены" или "Domains"
   - Найдите домен `luchik.by` в списке

3. **Откройте настройки домена**
   - Нажмите на домен `luchik.by` или кнопку "Управление" / "Настройки"
   - Откроется страница настроек домена

4. **Найдите раздел SSL**
   - Ищите вкладку или раздел "SSL", "SSL-сертификаты", "Безопасность"
   - Может быть в меню слева или в верхних вкладках

5. **Установите Let's Encrypt**
   - Найдите кнопку "Установить SSL", "Let's Encrypt", "Бесплатный SSL"
   - Нажмите на неё
   - Подтвердите установку

6. **Дождитесь активации**
   - Обычно это занимает 5-15 минут
   - Обновите страницу через несколько минут

### Способ 2: Через ISPmanager (если используется)

1. **Войдите в ISPmanager**
   - Обычно доступен по адресу: `https://ваш-сервер:1500` или через панель Hoster.by

2. **Перейдите в "WWW" → "Домены"**
   - В левом меню найдите "WWW"
   - Выберите "Домены"

3. **Откройте домен luchik.by**
   - Найдите `luchik.by` в списке
   - Нажмите на него

4. **Перейдите на вкладку "SSL"**
   - В верхней части страницы найдите вкладку "SSL"

5. **Установите Let's Encrypt**
   - Найдите опцию "Let's Encrypt" или "Автоматический SSL"
   - Нажмите "Установить" или "Создать"
   - Подтвердите установку

### Способ 3: Если не нашли раздел SSL

Если не можете найти раздел SSL в панели:

1. **Обратитесь в техподдержку Hoster.by**
   - Чат поддержки в панели управления
   - Email: support@hoster.by
   - Попросите установить бесплатный SSL-сертификат Let's Encrypt для домена luchik.by

2. **Укажите информацию:**
   - Домен: luchik.by
   - Нужен бесплатный SSL-сертификат Let's Encrypt
   - Домен уже привязан к хостингу

## После установки SSL

### 1. Проверьте работу HTTPS

1. **Откройте сайт через HTTPS:**
   - `https://luchik.by` (обязательно с https!)
   - Не используйте `http://luchik.by`

2. **Проверьте замочек в браузере:**
   - В адресной строке должен быть замочек 🔒
   - Нажмите на замочек, чтобы увидеть информацию о сертификате

3. **Проверьте через онлайн-сервисы:**
   - https://www.ssllabs.com/ssltest/analyze.html?d=luchik.by
   - Должна быть оценка A или выше

### 2. Включите принудительное перенаправление на HTTPS

После установки SSL рекомендуется перенаправлять всех посетителей на HTTPS.

**Обновите файл `.htaccess` на сервере:**

Откройте файл `/home/robotlidab/luchik.by/.htaccess` и раскомментируйте (уберите `#`) строки:

```apache
# Принудительное перенаправление на HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
```

Или добавьте эти строки в начало файла `.htaccess`:

```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteCond %{HTTPS} off
  RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
</IfModule>
```

### 3. Обновите ссылки на сайте

Убедитесь, что все ссылки на сайте используют `https://` вместо `http://`.

## Решение проблемы с Касперским

После установки SSL:

1. **Подождите 10-15 минут** после установки SSL
2. **Очистите кэш браузера** (Ctrl+Shift+Delete)
3. **Попробуйте открыть сайт снова:** `https://luchik.by`
4. **Если Касперский всё ещё блокирует:**
   - Добавьте сайт в исключения Касперского
   - Или временно отключите защиту для проверки

## Частые проблемы

### SSL не устанавливается

**Причины:**
- DNS ещё не обновился (подождите 24-48 часов)
- Домен не привязан к хостингу
- Проблемы с доступом к домену

**Решение:**
- Проверьте DNS: https://www.whatsmydns.net/#A/luchik.by
- Убедитесь, что домен правильно привязан
- Обратитесь в техподдержку Hoster.by

### SSL установлен, но сайт не открывается по HTTPS

**Решение:**
- Проверьте, что SSL действительно установлен (через панель управления)
- Подождите 15-30 минут для полной активации
- Очистите кэш браузера
- Попробуйте открыть в режиме инкогнито

### Предупреждение всё ещё появляется

**Решение:**
- Убедитесь, что открываете `https://luchik.by`, а не `http://`
- Проверьте срок действия сертификата
- Очистите кэш антивируса Касперского
- Добавьте сайт в исключения

## Контакты поддержки Hoster.by

Если не можете установить SSL самостоятельно:
- **Чат поддержки:** доступен в панели управления
- **Email:** support@hoster.by
- **Телефон:** указан в личном кабинете

Сообщите: "Нужна помощь с установкой бесплатного SSL-сертификата Let's Encrypt для домена luchik.by"


