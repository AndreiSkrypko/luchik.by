# Инструкция по деплою на Hoster.by

## Проблема: DNS_PROBE_FINISHED_NXDOMAIN

Эта ошибка означает, что домен `luchik.by` не настроен или не привязан к хостингу Hoster.by.

## Решение проблемы

### Шаг 1: Настройка DNS на Hoster.by

1. **Войдите в панель управления Hoster.by** (https://hoster.by)
2. **Перейдите в раздел "Домены" → "Управление DNS"** или **"DNS-зона"**
3. **Найдите домен `luchik.by`**

### Шаг 2: Проверьте DNS записи

Убедитесь, что у домена есть следующие записи:

**A-запись:**
- **Имя/Хост:** `@` или `luchik.by` или оставьте пустым
- **Значение/Points to:** IP-адрес вашего хостинга Hoster.by (узнайте в панели управления в разделе "Информация о хостинге")
- **TTL:** 3600 или Auto

**CNAME запись (для www):**
- **Имя/Хост:** `www`
- **Значение/Points to:** `luchik.by` или `@`
- **TTL:** 3600 или Auto

### Шаг 3: Если домен не привязан к Hoster.by

Если домен зарегистрирован у другого регистратора:

1. **Войдите в панель регистратора домена**
2. **Измените NS (Name Servers) записи на NS-серверы Hoster.by:**
   - Обычно это: `ns1.hoster.by` и `ns2.hoster.by`
   - Или уточните точные NS-серверы в панели Hoster.by в разделе "Домены"

3. **Или настройте A-запись:**
   - **Тип:** A
   - **Имя:** @ (или оставьте пустым)
   - **Значение:** IP-адрес хостинга Hoster.by (узнайте в панели управления)
   - **TTL:** 3600

### Шаг 4: Загрузка файлов на хостинг

**⚠️ ВАЖНО:** Корневая папка домена `luchik.by` находится по пути: `/home/robotlidab/luchik.by`

1. **Войдите в панель Hoster.by → "Файловый менеджер"** или **"File Manager"**
2. **Перейдите в папку домена:** `/home/robotlidab/luchik.by`
   - Это корневая папка для файлов сайта `luchik.by`
   - При удалении домена эта папка остается, но домен будет удален из аккаунта
3. **Удалите все файлы в этой папке** (если они есть, кроме `.htaccess` если он нужен)
4. **Загрузите файл `hoster.zip`** через файловый менеджер
5. **Распакуйте архив** (правой кнопкой → "Распаковать" / "Extract")
6. **Убедитесь, что все файлы находятся в корне папки `/home/robotlidab/luchik.by`** (не в подпапке)

### Шаг 6: Проверка файлов

После распаковки в `/home/robotlidab/luchik.by` должны быть:
- ✅ `index.html`
- ✅ `assets/` (папка)
- ✅ `img/` (папка)
- ✅ `.htaccess` (скрытый файл)
- ✅ `favicon.svg`
- ✅ `robots.txt`

### Шаг 7: Проверка прав доступа

Убедитесь, что файлы имеют правильные права:
- Папки: `755`
- Файлы: `644`
- `.htaccess`: `644`

### Шаг 8: Ожидание распространения DNS

После настройки DNS подождите **24-48 часов** для полного распространения изменений.

**Проверить DNS можно через:**
- https://www.whatsmydns.net/#A/luchik.by
- https://dnschecker.org/#A/luchik.by

### Шаг 9: Временная проверка через IP

Если нужно проверить сайт до настройки DNS:

1. **Узнайте IP-адрес хостинга** в панели Hoster.by (раздел "Информация о хостинге" или "Детали аккаунта")
2. **Добавьте в файл hosts** (Windows: `C:\Windows\System32\drivers\etc\hosts`):
   ```
   IP_АДРЕС_ХОСТИНГА luchik.by
   ```
   - Откройте файл от имени администратора
   - Добавьте строку в конец файла
   - Сохраните файл
3. **Откройте в браузере:** `http://luchik.by` или `http://IP_АДРЕС_ХОСТИНГА`

## Частые проблемы

### Сайт показывает ошибку 404
- Проверьте, что файлы в `/home/robotlidab/luchik.by`, а не в подпапке
- Убедитесь, что `.htaccess` присутствует
- Проверьте права доступа к файлам
- Убедитесь, что домен правильно привязан к папке `/home/robotlidab/luchik.by`

### Сайт показывает пустую страницу
- Откройте консоль браузера (F12) и проверьте ошибки
- Убедитесь, что все файлы загружены (особенно в папке `assets/`)
- Проверьте пути к файлам в `index.html`

### DNS не обновляется
- Подождите 24-48 часов
- Очистите DNS кэш: `ipconfig /flushdns` (Windows)
- Используйте другой DNS сервер (например, Google DNS: 8.8.8.8)

## Контакты поддержки Hoster.by

Если проблема не решается:
- **Техподдержка:** https://hoster.by/support или через панель управления
- **Телефон:** указан в личном кабинете
- **Email:** support@hoster.by
- **Чат поддержки:** доступен в панели управления Hoster.by

## Настройка SSL-сертификата (важно!)

Если вы видите предупреждение о недоверенном сертификате, нужно установить SSL:

### Вариант 1: Бесплатный SSL через Hoster.by (Let's Encrypt)

1. **Войдите в панель Hoster.by**
2. **Перейдите в "Домены" → найдите `luchik.by`**
3. **Нажмите на домен или "Управление"**
4. **Найдите раздел "SSL" или "SSL-сертификаты"**
5. **Выберите "Установить Let's Encrypt"** или **"Бесплатный SSL"**
6. **Подтвердите установку**
7. **Подождите 5-15 минут** для активации

### Вариант 2: Через ISPmanager

1. **Войдите в ISPmanager**
2. **Перейдите в "WWW" → "Домены"**
3. **Найдите `luchik.by` → нажмите на него**
4. **Вкладка "SSL"**
5. **Выберите "Let's Encrypt"** или **"Автоматический SSL"**
6. **Нажмите "Установить"**

### Вариант 3: Принудительное перенаправление на HTTPS

После установки SSL добавьте в `.htaccess`:

```apache
# Принудительное перенаправление на HTTPS
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteCond %{HTTPS} off
  RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
</IfModule>
```

### Проверка SSL

После установки проверьте:
- Откройте `https://luchik.by` (с https!)
- В браузере должен быть замочек 🔒
- Проверьте через: https://www.ssllabs.com/ssltest/analyze.html?d=luchik.by

## Дополнительная информация для Hoster.by

- **Панель управления:** обычно это ISPmanager или cPanel
- **Путь к файлам:** чаще всего `public_html` или `www`
- **PHP версия:** убедитесь, что используется PHP 7.4 или выше (для статических файлов не требуется)
- **Проверка DNS:** после настройки DNS проверьте через https://www.whatsmydns.net/#A/luchik.by
- **SSL-сертификат:** рекомендуется установить бесплатный Let's Encrypt через панель управления

